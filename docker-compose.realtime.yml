services:
  realtime-daemon:
    build:
      context: .
      dockerfile: Dockerfile.realtime
    env_file:
      - ./.env
    environment:
      # Inside a container, 'localhost' points to the container itself.
      # This overrides DATABASE_URL from .env so Prisma can reach the host MySQL.
      DATABASE_URL: "mysql://malec:TaiBiSiXiong707@host.docker.internal:3306/aergyle_game"
      NODE_ENV: development
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "3001:3001"
    restart: unless-stopped
